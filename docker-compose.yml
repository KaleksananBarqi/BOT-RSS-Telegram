version: '3.8'

services:
  rss-bot:
    build: .
    container_name: telegram_rss_bot
    restart: unless-stopped
    volumes:
      # Persist data (database)
      - ./data:/app/data
      # Mount .env file for credentials
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
